{
  "name": "talking-stickies-dev",
  "private": true,
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "start": "npm run build:happ && npm run start:agent",
    "start:agent": "cross-env HC_PORT=8888 ADMIN_PORT=$(port) concurrently \"npm run start:ui\" \"npm run start:happ\" \"sleep 5 && npm run start:ui\"",
    "start:ui": "npm run build:ui && npm run dev -w talking-stickies-app",
    "start:happ": "RUST_LOG=warn hc s -f=$ADMIN_PORT generate ./dna/workdir/happ/talking-stickies.happ --run=$HC_PORT -a talking-stickies network mdns",
    "build:happ": "npm run build:dna && hc app pack ./dna/workdir/happ",
    "build:dna": "npm run build:zome && hc dna pack ./dna/workdir/dna",
    "build:zome": "CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown",
    "build:ui": "npm run build -w talking-stickies-app",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@holochain-playground/cli": "^0.0.8",
    "concurrently": "^6.2.1",
    "concurrently-repeat": "^0.0.1",
    "cross-env": "^7.0.3",
    "json-parse-better-errors": "^1.0.2",
    "new-port-cli": "^1.0.0",
    "rimraf": "^3.0.2",
    "svelte-check": "^2.0.0",
    "svelte-preprocess": "^4.0.0",
    "@rollup/plugin-typescript": "^8.0.0",
    "typescript": "^4.0.0",
    "tslib": "^2.0.0",
    "@tsconfig/svelte": "^2.0.0"
  }
}